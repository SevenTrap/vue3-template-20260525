---
description: Pinia 状态管理命名与结构
globs: "src/store/**/*,**/store/**/*"
alwaysApply: false
---

# Pinia Store 约定

- 文件：`use<Domain>Store.js`，`defineStore` 的 id 使用 camelCase（如 `"geoMap"`）
- 复杂模块可拆分：`state.js`、`initStore.js`、`useXxxStore.js`

## 模式

```javascript
import { defineStore } from "pinia";

export const useGeoMapStore = defineStore("geoMap", {
  state: () => ({
    somePlugin: false, // 沿用项目拼写 visiable
  }),
  getters: {},
  actions: {
    /** @description 简要说明 @param {string} menuItem */
    TOGGLE_COMPONENT_VISIBLE(menuItem) {
      this[menuItem] = !this[menuItem];
    },
  },
});
```

## 约定

- **actions** 使用 `SCREAMING_SNAKE_CASE`（如 `SET_COMPONENT_VISIBLE_TRUE`）
- 控制插件/面板显隐的状态字段以 `Visiable` 结尾，与 `MenuBarPlugin` 的 key 对应
- 视图层通过 `mapState` / `storeToRefs` 或直接 `useXxxStore()` 访问，避免在 store 内引用 DOM 或 mars3d 实例

## 初始化

- 图表类 store 可提供 `initStore.js` 做默认 state，页面 `mounted` 时再拉取数据
