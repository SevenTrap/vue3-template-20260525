---
description: 项目总览与通用开发约定（Vue 3 + Vite GIS）
alwaysApply: true
---

# 项目总览

- **栈**：Vue 3 + Vite 8 + Pinia + Vue Router（Hash）+ Element Plus（zh-CN）
- **领域**：卫星轨道、Mars3D/Cesium 三维地球、OpenLayers、ECharts 态势图
- **路径别名**：`@` → `src/`（见 `vite.config.js`）
- **全局配置**：`public/configs/` 下脚本（如 `SYSTEM_CONFIG`、`MAP_CONFIG_*`），在 `index.html` 引入，页面内可直接使用

## 通用原则

- 改动范围尽量小，不重构无关文件
- 新功能沿用现有目录：`views/`、`store/`、`router/modules/`、`utils/`、`models/`、`plugins/`
- UI 文案与 Element Plus 语言包保持一致，使用中文
- 优先复用 `src/utils/service.js` 的 axios 实例，勿重复封装 HTTP 客户端
- 勿提交密钥；环境相关配置放 `public/configs/` 并由团队约定管理

## 布局与路由

| 布局 | 路径前缀 | 典型场景 |
|------|----------|----------|
| `layout` | `/layout` | 首页、模板、Element 示例 |
| `layoutGeo` | `/layoutGeo` | AI、Cesium、Geo 地图、OpenLayers |
| `layoutLeo` | `/layoutLeo` | 低轨 ECharts |

新增页面：在 `src/router/modules/` 增加路由数组，再挂到 `src/router/index.js` 对应 layout 的 `children`。
