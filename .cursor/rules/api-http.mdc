---
description: HTTP 请求与 API 模块约定
globs: "src/utils/service.js,**/apis/**/*"
alwaysApply: false
---

# API / HTTP 约定

## 客户端

- 使用 `src/utils/service.js` 导出的 axios 实例（已配置超时、拦截器、`userId` 请求头）
- 错误码文案在 `httpCode` 对象中，保持中文提示

## API 模块

- 按功能放在视图或组件旁的 `apis/index.js`，或 `src/views/<Feature>/apis/`
- 导出命名函数，内部调用 service：

```javascript
import service from "@/utils/service";

export function fetchSstLayer(params) {
  return service.get("/api/...", { params });
}
```

## 注意

- 流式/ SSE 场景可用 `@microsoft/fetch-event-source`（参考 `OllamaChat`）
- WebSocket 使用 `socket.io-client` 或 `ws` 时，在组件 `beforeUnmount` 断开连接
- 不要在新文件中 `axios.create()` 重复实例，除非有独立 baseURL 需求且经确认
