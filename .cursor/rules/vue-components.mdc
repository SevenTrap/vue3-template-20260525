---
description: Vue 单文件组件与页面结构约定
globs: "**/*.vue"
alwaysApply: false
---

# Vue 组件约定

本项目以 **Options API** 为主（`export default { name, data, mounted, methods }`），新页面请保持一致，勿擅自改为 `<script setup>` 除非用户明确要求。

## 结构

```vue
<template>
  <!-- 地图容器 ref 放最外层 -->
  <div ref="earthContainer" class="earth-container"></div>
  <!-- 插件/面板组件 -->
</template>

<script>
import { initViewer, globalViewer } from "@/utils/initEarth";
export default {
  name: "FeatureName",  // 与路由 name 或业务名一致
  components: { /* 局部插件 */ },
  data() { return {} },
  mounted() { /* 初始化地球/图表 */ },
  beforeUnmount() { /* 销毁 mars3d/echarts 实例、解绑事件 */ },
  methods: {},
};
</script>

<style lang="scss" scoped></style>
```

## 命名与注册

- 页面：`src/views/<Feature>/index.vue`
- 页面内插件：`src/views/<Feature>/components/*.vue`
- 跨页面复用：在 `src/components/register.js` 或 `src/plugins/register.js` 全局注册（`Vue.component(...)`）

## Element Plus

- 图标已全局注册 `@element-plus/icons-vue`，模板中直接用组件名
- 表单/表格样式扩展放在 `src/styles/elementPlus/`

## 注意

- 地图页在 `mounted` 中调用 `initViewer(container, MAP_CONFIG_*)`，使用 `globalViewer` 共享实例
- 插件面板关闭逻辑需在页面或 store 中统一 `handleClose`，与 `MenuBarPlugin` 联动
