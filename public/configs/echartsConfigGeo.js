window.echartsConfigGeo = {
  baseMapColor: "#9bc0f8",
  yAxisMin: -360,
  yAxisMax: 360,
  yAxisValues: [20, 40, 60, 80, 100, 200, 300, 600, 1500],
  viewConfig: [0, 180], //配置默认视窗经度范围
  gridLabelColor: "#000000",
  gridLineColor: "#f39800",
  zoomThrottle: 100,
  mapUrl: "/data/echartsMap/world.json",
  isMapShow: true, // 默认是否显示地图
};
